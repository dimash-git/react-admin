import Container from "@/components/container";

const ClauseSettingsLayout = ({ children }: { children: React.ReactNode }) => {
  return <Container>{children} </Container>;
};

export default ClauseSettingsLayout;
