import Container from "@/components/container";

const MyRouteLayout = ({ children }: { children: React.ReactNode }) => {
  return <Container>{children}</Container>;
};

export default MyRouteLayout;
